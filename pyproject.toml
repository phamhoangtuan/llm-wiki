[project]
name = "llm-wiki"
version = "0.1.0"
description = "LLM-maintained knowledge base — structured wiki with markdown + auto-generated HTML"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
]
